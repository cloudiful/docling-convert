name: Publish

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: true

jobs:
  publish:
    runs-on: ubuntu-latest
    environment:
      name: crates-io
      url: https://crates.io/crates/cloudiful-docling-convert
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Restore Rust cache
        uses: Swatinem/rust-cache@v2

      - name: Authenticate with crates.io
        id: auth
        uses: rust-lang/crates-io-auth-action@v1

      - name: Run workspace tests
        run: cargo test --workspace

      - name: Package core crate
        run: cargo package -p cloudiful-docling-convert --allow-dirty

      - name: Publish crate
        run: cargo publish -p cloudiful-docling-convert --registry crates-io --token "${{ steps.auth.outputs.token }}"
